spring:
  datasource:
    url: jdbc:h2:mem:sentinel_auth;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop

sentinel:
  security:
    jwt:
      secret: test-secret-with-more-than-32-bytes-for-hmac-signing

